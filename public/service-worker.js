// Questo file è necessario per rendere l'app installabile.
// Per ora può rimanere vuoto, la sua presenza è ciò che conta.

self.addEventListener('fetch', (event) => {
  // Per il momento non aggiungiamo funzionalità offline.
  // Il service worker serve solo a soddisfare i requisiti di una PWA.
});